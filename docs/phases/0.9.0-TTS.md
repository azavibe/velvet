You are working in the `azavibe/velvet` repository. The product is currently named Aral and is built from the Whisperi codebase.

Use GPT‑5.6 Luna with max reasoning effort.

This is a planning turn. Do not modify code yet. Produce a repository-grounded, implementation-ready plan that can be executed incrementally in later turns.

Before planning, read the implementation—not only the documentation. At minimum inspect:

* `AGENTS.md`
* `CLAUDE.md`
* `.github/README.md`
* `docs/ARCHITECTURE.md`
* `docs/CHANGELOG.md`, especially version `0.8.8`
* `docs/TODO.md`
* `src/App.tsx`
* `src/components/ConversationWindow.tsx`
* `src/components/settings/SettingsPanel.tsx`
* `src/components/settings/StatisticsSection.tsx`
* `src/components/settings/ConversationsSection.tsx`
* `src/components/settings/TranscriptionSection.tsx`
* `src/components/settings/AIModelsSection.tsx`
* `src/components/settings/DictionarySection.tsx`
* `src/components/settings/AgentSection.tsx`
* `src/hooks/useAudioRecording.ts`
* `src/hooks/useTranscriptionPipeline.ts`
* `src/hooks/useConversation.ts`
* `src/hooks/useNoteCapture.ts`
* `src/config/voiceCommands.ts`
* `src/config/prompts.ts`
* `src/models/dictionary.ts`
* `src/models/modelRegistryData.json`
* `src/services/tauriApi.ts`
* `src-tauri/src/audio/`
* `src-tauri/src/transcription/`
* `src-tauri/src/reasoning/`
* `src-tauri/src/commands/database.rs`
* `src-tauri/src/commands/conversation.rs`
* `src-tauri/src/commands/notes.rs`
* `src-tauri/src/database/mod.rs`
* `src-tauri/src/database/migrations.rs`
* `src-tauri/src/lib.rs`

If documentation conflicts with implementation, treat the current code, migrations, and latest changelog as authoritative and note the discrepancy.

## Existing functionality that must not be rebuilt

The repository already has all of the following:

* Cloud transcription providers: OpenAI, Groq, Mistral, Qwen, and OpenRouter.
* Cloud enhancement providers including OpenAI-compatible providers, Anthropic, Gemini, Groq, Qwen, and OpenRouter.
* Separate provider/model settings for transcription, enhancement, and conversation suggestions.
* A custom dictionary with canonical terms, likely ASR aliases, and contextual/always replacement policies.
* Configurable agent name and aliases. The current default name is `Aral`, but no new logic may hard-code that name.
* Agent-address detection for conversational enhancement.
* Whole-utterance voice commands such as:

  * “Aral, start notes”
  * “Aral, start conversation”
  * “Aral, support”
  * “Aral, stop”
* Forgiving wake-word matching for common ASR variations such as “Aro,” “Ahral,” and “Arrow.”
* A dedicated Conversation window.
* Dual-channel conversation capture using microphone and Windows system-audio loopback.
* Conversation personas.
* Automatic or hotkey-triggered conversation suggestions.
* A conversation suggestion model that is configured independently from the normal enhancement model.
* Notes with start, pause, resume, append, cleanup, and history integration.
* History & Analytics combining dictations, conversations, and notes.
* SQLite persistence for dictations, conversation utterances, suggestions, notes, durations, and audio paths.
* Local WAV archiving for dictations, conversation microphone/loopback channels, and notes.

Do not propose a second provider framework, a second dictionary, a parallel conversation system, or a replacement history database. Extend and reuse the existing implementation.

Do not invent a fixed JSON response contract or prescribe generic DTOs before examining the repository’s existing Rust and TypeScript types. Choose internal structures that fit the current code.

## Objective

Plan how to evolve the existing application into a low-cost conversational voice assistant that:

1. Continues working as normal dictation.
2. Automatically improves likely accent-related and contextual ASR mistakes without requiring correction forms.
3. Lets the user address the configured agent and receive an actual conversational answer.
4. Speaks that answer through OpenRouter text-to-speech.
5. Remembers useful conversational context over time.
6. Reuses the existing Conversation window, reasoning settings, history, SQLite database, recording archive, and audio infrastructure as much as practical.
7. Remains cloud-only.
8. Preserves a path toward disclosed AI participation in online meetings.

The immediate implementation must not attempt full Zoom or Teams participation.

## First: investigate and repair audio history

Audio persistence is already intended to work.

`useAudioRecording.ts` passes the WAV bytes to `saveTranscription()`. The backend writes dictation audio under the application recordings directory and later updates `transcriptions.audio_path`. Conversations already have `audio_path_me` and `audio_path_them`. Notes already have `audio_path`.

However, History & Analytics currently exposes text actions but no clear audio playback or open-file controls. Dictation audio is also written asynchronously, which means the first history result can contain `audio_path: null` and remain stale after the file finishes writing.

Treat this as an existing feature with a bug/incomplete UI, not as a new audio-storage project.

The plan must investigate:

* Whether WAV files are actually written successfully in normal runtime conditions.
* Whether database paths are updated after background writes.
* The race between history loading and the asynchronous `audio_path` update.
* Whether save failures are visible anywhere.
* Whether paths returned through Tauri are safe and usable by the WebView.
* Why History & Analytics cannot currently play or open the archived audio.
* The existing note limitation where appending a later recording replaces the linked audio path while older segments remain on disk.

Plan a repair that provides:

* Play, pause, resume, seek, restart, and stop.
* Correct duration display when available.
* One recording for dictations.
* Separate “Me” and “Them” tracks for conversations.
* Note audio without silently orphaning earlier append segments.
* Clear handling for missing, still-saving, corrupt, or deleted files.
* Proper cancellation when switching history items.
* Secure access to files under the application recordings directory only.
* Deletion behavior that removes every associated recording.
* A reusable playback layer that can also play newly synthesized TTS audio.

Do not build two unrelated audio players for history and TTS.

## Agent-name transcription bug

The configured name is `Aral`, although ASR may return variants such as `Aro`, `Aarrol`, `Ahral`, or repeated sequences.

A reported failure looks like:

`...now I have Aral Aro Aro Aro Aro`

Trace the complete interaction between:

* `buildTranscriptionDictionary()`
* Whisper prompt construction
* Agent-name and alias protection
* `strip_prompt_echo()`
* `strip_dictionary_edge_echo()`
* The hallucination blocklist
* `detectVoiceCommand()`
* Conversational address detection

The agent name is currently added to transcription vocabulary and protected from echo removal. That may help recognition, but it can also preserve a repeated prompt-conditioned hallucination at the end of a valid transcript.

Plan a conservative fix that:

* Preserves a genuinely spoken invocation such as “Aral, start notes.”
* Continues accepting common wake-word misrecognitions for voice commands.
* Removes repeated agent-name variants from transcript edges.
* Removes a repeated trailing wake-word hallucination after a real sentence.
* Does not remove a legitimate single interior mention.
* Does not globally replace ordinary words merely because they sound similar to the agent name.
* Evaluates whether the agent name should continue being sent in Whisper’s prompt, given that voice-command matching is already fuzzy.
* Includes regression tests for leading invocation, trailing echo, repeated variants, punctuation, casing, aliases, silence, ordinary interior mentions, and multilingual utterances.

Keep this fix independent and releasable.

## Automatic accent and context adaptation

The user must not be required to fill in correction forms, type training examples, or manually maintain mappings.

The existing dictionary remains available for explicit user overrides, but the new adaptation layer should work automatically in the background.

Plan a conservative reconciliation step after ASR and before command detection, enhancement, persistence, or conversational reasoning. It should use available evidence such as:

* The raw transcription.
* Detected/configured language.
* The current conversation or note.
* Recent relevant utterances.
* Existing dictionary terms and aliases.
* Known agent-name variants.
* Conversation participants, personas, project names, products, services, and other known entities.
* Previously observed high-confidence ASR confusions.
* Repeated evidence from separate utterances or sessions.

Required behavior:

* Correct only when context provides strong evidence.
* Preserve the speaker’s meaning and wording.
* Prefer corrections of proper nouns and known multiword entities.
* Do not learn a permanent global replacement from one occurrence.
* Keep uncertain corrections temporary and local to the current context.
* Promote learned corrections only after repeated consistent evidence.
* Reduce or remove confidence after contradictions or long periods without support.
* Preserve the raw ASR transcript alongside the reconciled text.
* Make automatic behavior reversible and debuggable.
* Avoid feedback loops where a model’s earlier guess becomes proof of its own correctness.
* Keep normal enhancement and contextual ASR reconciliation as separate responsibilities.
* Do not introduce a routine manual correction screen.

Explain in the plan that this is contextual post-ASR adaptation. It does not fine-tune Groq Whisper’s acoustic weights to the user’s accent.

Use an economical configurable cloud reasoning model. Reuse the existing provider and API-key infrastructure instead of hard-coding another vendor.

## Direct conversation with the agent

The current application has two related but distinct behaviors:

* Addressing the agent during normal dictation routes through conversational enhancement.
* The Conversation window captures calls and generates suggested replies from the accumulated transcript.

Plan a coherent extension instead of creating a third disconnected chat feature.

Default behavior should be:

* Ordinary dictation remains dictation: transcribe, optionally enhance, save, and paste. It is not spoken aloud.
* Existing whole-utterance voice commands continue performing application actions.
* A non-command utterance addressed to the configured agent becomes a conversational turn.
* The agent’s answer is visible as text and spoken through TTS.
* A conversational answer is not pasted into the focused application as ordinary dictated text unless existing settings explicitly request that behavior.
* User and assistant turns are persisted so the conversation can continue naturally.
* The existing Conversation window should display the exchange or be extended to support the direct-assistant use case.
* Reuse the existing conversation reasoning provider/model setting by default instead of silently using the normal cleanup model.
* Existing meeting/copilot suggestions must continue working.

Determine from the code whether direct agent conversation should reuse the current `conversations` and `conversation_utterances` tables or whether a small compatible extension is cleaner. Avoid duplicating stored transcripts.

## OpenRouter text-to-speech

Extend the existing OpenRouter integration to support its speech endpoint:

`POST https://openrouter.ai/api/v1/audio/speech`

Use the already configured OpenRouter API key.

Default model:

`qwen/qwen-audio-3.0-tts-flash`

Allow the user to switch to another compatible OpenRouter TTS model, including a custom model identifier, using the same provider/model selection conventions already used elsewhere in the application.

Plan for:

* MP3 or another format that is reliable in the current Tauri WebView.
* Receiving and validating the raw audio response.
* Playing it through the reusable audio playback layer.
* Immediate stop/cancel.
* Stopping speech when the user starts speaking again.
* Ignoring stale synthesis responses after cancellation or a newer turn.
* Keeping the text response visible when synthesis fails.
* Authentication, timeouts, rate limits, malformed audio, non-audio error bodies, unavailable models, and network failures.
* Avoiding API-key and audio-data logging.
* Preventing synthesized speech from being transcribed as new user speech.
* Correct interaction with microphone capture and conversation loopback capture.
* A small TTS setting in the existing Conversations area, not a new top-level settings system.
* Model discovery through OpenRouter’s speech-output model metadata where practical, with a cached fallback so settings still render when discovery is unavailable.

Voice cloning is not required for the first TTS milestone. Do not claim that the Qwen endpoint supports reference-audio cloning unless OpenRouter’s current endpoint capability metadata confirms it. Keep the implementation compatible with future preset voices, enrolled voice IDs, or consenting reference audio without building a speculative voice-cloning framework now.

Use current official OpenRouter documentation while planning:

* https://openrouter.ai/docs/guides/overview/multimodal/tts
* https://openrouter.ai/qwen/qwen-audio-3.0-tts-flash

## Conversation memory and knowledge

Build on the data already stored in SQLite:

* Dictation history.
* Conversations.
* Conversation utterances.
* Conversation suggestions.
* Notes.
* Personas.
* Audio attachment paths.

Do not introduce Neo4j or another server.

Plan a minimal SQLite-backed memory layer that can represent useful entities, aliases, facts, and relationships while retaining source, confidence, timestamps, and contradiction state. Derive its exact schema from the repository’s existing migration and database conventions.

The first useful memory should support things such as:

* People and participant names.
* The agent.
* Organizations.
* Projects and products.
* Services and models.
* Topics.
* Decisions.
* Tasks.
* Known terminology and aliases.
* Context-specific speech confusions.

Memory creation should be automatic after completed conversational turns, notes, or conversations. Ambiguous content must not immediately become permanent memory.

Do not send the entire history or database to the reasoning model. Plan compact retrieval using:

* The active session.
* A bounded recent-turn window.
* A rolling summary.
* SQLite search/indexing.
* Entity aliases.
* Recency.
* Confidence.
* Current persona, meeting, or topic.

Memory deletion must follow deletion of its source when appropriate. Include a simple automatic-memory toggle and clear/reset action, but no graph editor or training interface.

## Interruption and anti-feedback

Extend the current recording phases carefully. The plan must account for:

* Listening.
* Transcribing.
* Reconciling.
* Thinking.
* Synthesizing.
* Speaking.
* Interrupted.
* Failed but recoverable.

Define cancellation ownership and stale-result protection using patterns consistent with the existing React hooks and Tauri commands.

When the user starts speaking:

* Current TTS playback stops immediately.
* Pending synthesis becomes stale.
* The new user turn is accepted.
* An old reasoning or TTS response must not begin playing later.
* The app must not transcribe its own speaker output as a new command or user turn.

Do not perform a large state-management rewrite unless the current hooks genuinely cannot support the required coordination.

## Future meeting participation

Do not implement virtual microphones or autonomous meeting participation in the initial work.

Ensure the design does not block a later disclosed meeting-participant mode using:

* Existing mic and loopback capture.
* Existing two-channel transcripts.
* Existing personas and conversation suggestions.
* TTS output.
* Barge-in.
* Echo cancellation or synthesized-output exclusion.
* A virtual microphone for Zoom or Teams.
* Participant and meeting-title context.
* Meeting summaries, decisions, and tasks.
* Clear visible listening/speaking state.
* Explicit participant disclosure and consent.

The application must never covertly impersonate the user. If a future cloned voice is used in a meeting, participants must be informed that the voice is AI-generated.

## Planning requirements

Produce an implementation plan based on the repository’s actual code. Include:

1. A corrected current-state map with exact file references.
2. Confirmed root causes and unresolved questions, clearly distinguishing evidence from hypotheses.
3. The smallest coherent architecture that reuses existing modules.
4. Database migrations required, following the existing migration style.
5. Existing Rust modules, Tauri commands, React hooks, components, settings, and types to modify.
6. New files only where an existing responsibility cannot reasonably own the change.
7. The audio-history repair and how its playback layer will be reused by TTS.
8. Direct agent-conversation routing and persistence.
9. Automatic contextual correction and learning behavior.
10. Memory retrieval, summarization, confidence, contradiction, deletion, and cost controls.
11. TTS request, playback, cancellation, and anti-feedback behavior.
12. Privacy, local audio retention, API-key security, and meeting consent.
13. Unit, integration, regression, and Windows manual tests.
14. Documentation updates required by `CLAUDE.md`.
15. Updates for all nine locale files whenever UI strings are added.
16. Safe commit boundaries and verification criteria for every milestone.
17. Explicit non-goals and features deferred to later work.

Do not include sample JSON contracts merely to make the plan look detailed. Use the repository’s current type patterns and explain behavior, ownership, and data flow.

## Recommended milestones

Keep every milestone independently testable and suitable for a separate commit or small commit series:

1. Establish a clean baseline and record current test results.
2. Repair archived-audio persistence visibility and add playback to History & Analytics.
3. Fix repeated Aral/agent-name transcription echoes.
4. Introduce persistent direct conversational turns using existing conversation infrastructure.
5. Add OpenRouter TTS with Qwen Flash and reusable playback cancellation.
6. Add barge-in, stale-response protection, and synthesized-audio exclusion.
7. Add automatic conservative contextual transcription reconciliation.
8. Add bounded conversation memory and rolling summaries.
9. Add the minimal SQLite knowledge layer and automatic extraction.
10. Evaluate better streaming audio and voice options.
11. Design virtual-microphone meeting participation separately.

Do not combine all milestones into one implementation change.

## Verification

For each applicable milestone, include the exact repository commands needed, including:

* `bun test`
* `bun run typecheck`
* `bun run build`
* `cargo fmt --check`
* `cargo test`
* `cargo clippy`

Also include Windows-specific manual verification for:

* Microphone dictation.
* Agent voice commands.
* Direct spoken conversation.
* History audio playback.
* Conversation “Me” and “Them” recordings.
* Note audio and appended note segments.
* TTS playback.
* TTS interruption.
* Speaker-output feedback prevention.
* Missing API keys and provider failures.

End the plan with the recommended first implementation milestone only. Do not start coding in this turn.


#### TEchinacal Plan
Read docs/phases/0.9.0-TTS.md. It explicitly requires a planning-only pass, so I did not modify
  source code.

  Current baseline:

  - Worktree has only the existing untracked docs/phases/ directory.
  - bun test: 25 passing.
  - bun run typecheck: blocked because tsc is unavailable.
  - cargo fmt --check: fails on existing formatting differences.
  - cargo test: blocked by unavailable crates.io access.

  ## Current implementation map

  - /C:/Dev/agenda/src/hooks/useAudioRecording.ts records, transcribes, detects commands,
    enhances, pastes, and saves dictation.

  - /C:/Dev/agenda/src-tauri/src/commands/database.rs inserts the transcription immediately, then
    writes its WAV and updates audio_path in a detached background task.

  - /C:/Dev/agenda/src/components/settings/StatisticsSection.tsx loads history once and has no
    playback, refresh, or archive-status handling.

  - Conversation and note archives in /C:/Dev/agenda/src-tauri/src/commands/conversation.rs and /
    C:/Dev/agenda/src-tauri/src/commands/notes.rs also use detached archive tasks. Note append
    currently replaces the single stored path, leaving older segments orphaned.

  - /C:/Dev/agenda/src/services/tauriApi.ts exposes no secure audio retrieval/opening API and no
    TTS API.

  - Agent names are added to protected ASR terms in /C:/Dev/agenda/src/hooks/
    useTranscriptionPipeline.ts, while Rust prompt/dictionary echo stripping runs before frontend
    command routing.

  - Addressed speech currently only selects the chat enhancement prompt. It is still pasted and
    saved as ordinary dictation.

  - Conversations already have SQLite persistence, dual-channel audio, personas, suggestions, and
    a dedicated window, but no assistant-turn channel.

  - There are no memory tables or TTS settings.
  - The actual app has three windows, while parts of /C:/Dev/agenda/docs/ARCHITECTURE.md still
    describe the older two-window/schema model.

  ## Root causes

  1. Audio paths are eventually consistent, but History has no completion event or reload.
  2. Conversation/note archive writes and finalization can race because each is detached
     independently.

  3. Failure states are only logged; the UI cannot distinguish saving, missing, corrupt, or failed
     audio.

  4. Notes have one path despite supporting multiple append segments.
  5. There is no safe WebView-facing file source or reusable player.
  6. Agent names are protected from generic echo removal, preserving repeated wake-word
     hallucinations.

  7. Chat-mode detection happens too late and is treated as enhancement rather than a separate
     conversational route.

  8. The existing suggestion path sends an unbounded full conversation to the reasoning provider.
  9. OpenRouter speech model discovery, raw-audio validation, cancellation, and playback do not
     exist.

  ## Target architecture

  The pipeline should become:

  record
    → cloud ASR
    → contextual reconciliation
    → voice-command routing
    → ordinary dictation OR direct conversation
    → persistence
    → visible result
    → optional TTS

  Use explicit phases:

  Listening → Transcribing → Reconciling → Thinking → Synthesizing → Speaking

  with recoverable Interrupted and Failed states.

  Every operation gets a generation/request ID. Starting new speech increments the generation,
  stops playback immediately, and causes late ASR/reasoning/TTS results to be ignored.

  ### Audio history repair

  Add a migration-backed audio_assets table containing:

  - owner type and owner ID;
  - track (main, me, or them);
  - validated path;
  - MIME type, byte length, duration;
  - status: saving, ready, missing, or failed;
  - error text and timestamps.

  Backfill existing transcription, conversation, and note paths where possible. Keep legacy path
  columns temporarily for compatibility, but make audio_assets canonical.

  Replace detached archive writes with an ordered archive coordinator:

  - dictation save waits for the WAV write and database update;
  - conversation chunk writes are serialized;
  - finalization waits for all queued chunks;
  - note append creates a tracked segment rather than overwriting one path;
  - deletion removes every linked asset and file.

  Expose only asset IDs through the frontend. A backend command validates that the canonical path
  remains inside the application recordings directory before returning a Tauri asset URL or
  opening it. Never allow arbitrary frontend paths.

  Create one reusable frontend playback layer supporting:

  - play, pause, resume, seek, restart, stop;
  - duration and current position;
  - one dictation recording;
  - ordered note segments;
  - separate conversation Me and Them tracks;
  - cancellation when switching history items;
  - missing/corrupt/deleted handling.

  Use the same controller for TTS blobs.

  ### Agent echo repair

  Add a dedicated agent-echo sanitizer after ASR cleanup but before command and chat routing.

  Rules:

  - preserve Aral, start notes and equivalent command invocations;
  - collapse repeated leading wake words;
  - remove repeated trailing wake words;
  - preserve one meaningful leading address for direct conversation;
  - preserve interior mentions;
  - handle punctuation, case, aliases, and fuzzy variants;
  - never perform global sound-alike replacement.

  Add Rust and TypeScript tests for command, chat, ordinary dictation, repeated names, aliases,
  silence, punctuation, and multilingual text.

  ### Direct conversation

  Extend the existing conversation tables rather than creating another chat database:

  - add conversations.kind with meeting and direct;
  - add nullable conversation_utterances.raw_text;
  - allow assistant as an utterance channel;
  - retain reconciled text in the existing text field;
  - optionally associate each direct user recording with an audio asset.

  Addressed non-command speech should:

  - create or reuse a direct conversation;
  - persist the user turn;
  - call the existing conversation reasoning provider/model;
  - persist the assistant answer;
  - show the conversation window;
  - speak the answer when TTS is enabled;
  - never paste the answer as ordinary dictation.

  Existing meeting capture, personas, suggestions, consent, and two-channel behavior remain
  unchanged.

  Bounded context should use recent turns, a rolling summary, active persona, and retrieved memory
  rather than the entire conversation.

  ### Contextual ASR reconciliation

  Add a conservative reconciliation stage before command detection, enhancement, and persistence.

  Evidence may include:

  - raw ASR text;
  - detected language;
  - current mode/persona;
  - recent utterances;
  - explicit dictionary entries;
  - agent name and aliases;
  - known entities;
  - previously confirmed high-confidence confusions.

  The stage must:

  - preserve raw ASR;
  - return the raw text on uncertainty, timeout, or provider failure;
  - protect proper nouns and multiword terms;
  - avoid global replacement;
  - avoid learning from one unsupported correction;
  - decay or contradict old observations;
  - remain reversible and debug-visible;
  - never feed its own output back as raw evidence.

  Add transcriptions.reconciled_text, plus equivalent raw/reconciled fields for conversation and
  notes.

  Use the existing reasoning provider/key infrastructure and its configured economical reasoning
  model. This is contextual post-processing, not acoustic-model fine-tuning.

  ### OpenRouter TTS

  Implement TTS in the existing Rust HTTP/provider layer using:

  POST https://openrouter.ai/api/v1/audio/speech

  The official OpenRouter API returns raw audio bytes and supports MP3/PCM response formats.
  Request MP3 for normal playback and validate both status and audio content before returning it.
  Model discovery should use the speech-output metadata endpoint:

  GET /api/v1/models?output_modalities=speech

  See the OpenRouter TTS documentation
  (https://openrouter.ai/docs/guides/overview/multimodal/tts).

  Use the existing OpenRouter key and default model:

  qwen/qwen-audio-3.0-tts-flash

  The official Qwen model page (https://openrouter.ai/qwen/qwen-audio-3.0-tts-flash) should be
  treated as descriptive only; do not assume voice IDs or voice cloning support unless the live
  model metadata confirms them.

  Add a small Conversations setting:

  - TTS enabled/disabled;
  - OpenRouter TTS model with discovered and custom model support;
  - cached metadata with a safe default fallback.

  TTS failures must leave visible text intact and expose a localized, non-sensitive error. Do not
  log keys, audio bytes, or raw provider bodies.

  ### Interruption and anti-feedback

  When new recording begins:

  1. emit a cross-window speech-interrupt event;
  2. stop the current audio element;
  3. cancel or invalidate pending synthesis;
  4. start the new microphone capture;
  5. ignore late speech results from the old request.

  Prevent synthesized output from becoming new input through:

  - a short post-stop microphone suppression window;
  - normalized matching against the most recent assistant output;
  - no TTS while the existing loopback meeting capture is active unless explicitly designed and
    disclosed;

  - explicit UI indication that the voice is AI-generated.

  ### Memory

  Use SQLite, not Neo4j.

  Add migration-backed tables for:

  - entities;
  - aliases;
  - facts/relationships;
  - confidence and contradiction state;
  - source references;
  - timestamps and recency;
  - rolling conversation summaries.

  Extraction runs after completed direct turns, notes, and conversations only when automatic
  memory is enabled. Retrieval is bounded by:

  - current session;
  - recent turns;
  - rolling summary;
  - relevant SQLite entities/facts;
  - recency, confidence, persona, and topic.

  Useful memory types include people, organizations, projects, products, services, models, topics,
  decisions, tasks, terminology, aliases, and recurring ASR confusions.

  Deleting a source must remove or downgrade facts that depend solely on that source. Add simple
  “automatic memory” and “reset memory” controls; do not build a graph editor or training UI.

  ## Planned migrations

  1. v6 — audio assets

     Add audio_assets, status tracking, segment support, and compatibility backfill from existing
     path columns.

  2. v7 — direct turns and reconciliation

     Add conversation kind, raw utterance text, assistant channel support, and reconciled
     transcription fields.

  3. v8 — memory

     Add entities, aliases, facts, sources, confidence/contradiction fields, and rolling
     summaries.

  Use the existing sequential user_version migration style in /C:/Dev/agenda/src-tauri/src/
  database/migrations.rs, with tests for fresh databases and upgrades from v1–v5.

  ## File plan

  Likely modified files:

  - src/hooks/useAudioRecording.ts
  - src/hooks/useTranscriptionPipeline.ts
  - src/hooks/useConversation.ts
  - src/components/settings/StatisticsSection.tsx
  - src/components/settings/ConversationsSection.tsx
  - src/components/ConversationWindow.tsx
  - src/components/settings/TranscriptionSection.tsx
  - src/services/tauriApi.ts
  - src/config/prompts.ts
  - src/config/voiceCommands.ts
  - src/models/modelRegistryData.json
  - all nine locale files under src/i18n/locales/
  - src-tauri/src/commands/database.rs
  - src-tauri/src/commands/conversation.rs
  - src-tauri/src/commands/notes.rs
  - src-tauri/src/commands/transcription.rs
  - src-tauri/src/commands/reasoning.rs
  - src-tauri/src/database/mod.rs
  - src-tauri/src/database/migrations.rs
  - src-tauri/src/transcription/cloud.rs
  - src-tauri/src/http.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/tauri.conf.json

  Likely new files:

  - src/hooks/useAudioPlayback.ts
  - src/services/audioPlayback.ts
  - src-tauri/src/audio/tts.rs
  - src-tauri/src/commands/tts.rs
  - a focused reconciliation module;
  - memory/database helpers only if existing modules become unwieldy.

  Do not add a second provider framework, dictionary system, conversation store, or history
  database.

  ## Verification plan

  Frontend:

  - existing bun test;
  - agent-echo unit tests;
  - direct-routing tests;
  - reconciliation safety and fallback tests;
  - playback state/cancellation tests;
  - TTS response/error/catalog tests;
  - bun run typecheck;
  - bun run build.

  Rust:

  - migration upgrade/fresh-database tests;
  - audio archive ordering/finalization tests;
  - path confinement tests;
  - deletion and missing-file tests;
  - WAV/MP3 validation tests;
  - direct-turn persistence tests;
  - memory confidence/contradiction/reset tests;
  - cargo fmt --check;
  - cargo test;
  - cargo clippy --all-targets --all-features -- -D warnings.

  Windows manual checks:

  - first-run recording directory creation;
  - dictation audio immediately visible in History;
  - playback controls and seeking;
  - conversation Me/Them playback;
  - note append playback and deletion;
  - missing/corrupt files;
  - switching items during playback;
  - direct answer visibility and no paste;
  - TTS interruption/barge-in;
  - synthesized speech not re-transcribed;
  - all three windows;
  - all nine locales;
  - meeting consent and AI-voice disclosure.

  ## Commit boundaries

  1. fix(audio): serialize archive completion and add secure history playback
  2. fix(asr): remove repeated agent wake-word echoes
  3. feat(conversation): persist addressed direct turns
  4. feat(tts): add OpenRouter speech synthesis and playback
  5. fix(voice): interrupt TTS and suppress synthesized echo
  6. feat(asr): add contextual reconciliation
  7. feat(memory): add bounded SQLite memory and summaries
  8. docs: document 0.9.0 architecture, privacy, and deferred meeting work

  Each feature commit should include its English strings and matching zh, ja, ko, de, fr, es, pt,
  and ru translations.

  Final documentation updates should cover /C:/Dev/agenda/CLAUDE.md, /C:/Dev/agenda/docs/
  ARCHITECTURE.md, /C:/Dev/agenda/docs/CHANGELOG.md, /C:/Dev/agenda/docs/TODO.md, /C:/Dev/agenda/
  docs/PROGRESS.md, and the release-facing README where appropriate. The 0.9.0 changelog entry
  must begin with ### Highlights and contain 1–4 plain-English bullets.

  Deferred work:

  - voice cloning and reference-audio UX;
  - streaming TTS;
  - virtual microphones;
  - autonomous Zoom/Teams operation;
  - covert impersonation;
  - local ASR/TTS models;
  - Neo4j or graph-editor UI;
  - manual correction/training screens.

  ## Recommended first implementation milestone

  Milestone 1: durable audio history and reusable playback

  Implement the v6 audio-assets migration, ordered archive completion, secure recordings access,
  History refresh/status handling, and the shared playback controller. Acceptance requires:

  - new dictation rows expose ready audio before the save command completes;
  - conversation and note archives finalize after all queued chunks;
  - note append segments are all tracked and deletable;
  - playback supports play/pause/resume/seek/restart/stop;
  - missing and corrupt files are recoverable UI states;
  - deletion removes every associated recording;
  - playback code is reusable by the later TTS milestone.
